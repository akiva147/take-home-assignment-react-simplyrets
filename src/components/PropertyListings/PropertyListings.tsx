import classes from './property-listings.module.scss';

export interface PropertyListingsProps {}

export const PropertyListings = (props: PropertyListingsProps) => {
  return <div>It's PropertyListings PAge (:</div>;
};
